# 红警2网页版 · 增强 AI 部署指南（中文）

> 本仓库 = ra2web（红色井界 / 王二火大）官方静态部署包 v0.87.0 + **增强版电脑 AI** + **自建联机大厅支持**。
> 原始部署包来源：[ra2web/ra2web.github.io](https://github.com/ra2web/ra2web.github.io)
> 配套自建大厅服务端：[leoandagents/ra2web-wol-server](https://github.com/leoandagents/ra2web-wol-server)（联机对战平台，含注册/大厅/游戏中继）

## 快速导航

- [AI 难度分级说明](#和原版有什么区别)
- [部署方式 A：GitHub Pages](#部署方式-agithub-pages免费最简单)
- [部署方式 B：VPS + Nginx](#部署方式-b自己的-vps--nginx推荐可联机)
- **[自建联机大厅（和朋友在线对战）](#自建联机大厅和朋友在线对战)**
- [部署方式 C：本地 Windows](#部署方式-c本地-windows-跑一下)
- [常见问题](#常见问题)
- [合规说明](#合规说明请遵守)

---

## 和原版有什么区别

本仓库把四个电脑难度档重做为**四级天梯**（开局聊天框会显示当前档位）：

| 档位 | 内容 |
|---|---|
| AI-入门 | 官方原版 AI（即官方最强档"老牧师"本尊） |
| AI-标准 | 增强版 **Lite**：现金门槛（缺钱先发展经济）、固定开局建造顺序、护矿 |
| AI-困难 | 增强版 **Standard** = Lite + 更快的防守响应 |
| AI-噩梦 | 增强版 **Max** = Standard + 骚扰突袭小队（专偷矿车）+ 常驻侦察 + 更高操作频率 |

**验证方式**：增强版开局会显示 `[增强版AI] Lite/Standard/Max 已加载`；入门档（官方 AI）无此宣言。

所有增强版对官方 AI 的自动对战胜率约 70-75%（每版经 20-50 局无头 A/B 对战验证，见 bot 源码仓库的 experiments.md）。

原版的三个 AI 文件保留在 `js/bot1.js.orig`、`js/bot2.js.orig`、`js/bot3.js.orig`（bot4.js 即官方 AI，正在服役于入门档）。

---

## 部署方式 A：GitHub Pages（免费，最简单）

⚠️ 因为站点资源用绝对路径加载，**必须用主站形式的仓库名**，否则会 404：

1. 新建一个名为 **`<你的用户名>.github.io`** 的仓库（如果已有则占用，需换个方式）
2. 把本仓库的所有文件传上去（Code → Upload files 全选上传，或 git push）
3. 等 1-2 分钟，访问 `https://<你的用户名>.github.io/`
4. 首次进入会提示下载游戏资源，点「使用 CDN 资源」即可

如果你已经用过主站 Pages：改用下面任一方式，或在仓库 Settings → Pages 绑定一个**自定义域名**（子路径部署会打不开）。

## 部署方式 B：自己的 VPS + Nginx（推荐，可联机）

适合想让朋友随时访问的情况。以 Ubuntu/Debian 为例：

```bash
# 1. 安装 nginx
sudo apt update && sudo apt install -y nginx git

# 2. 拉取站点文件
sudo git clone <本仓库地址> /var/www/ra2web

# 3. 配置 nginx
sudo tee /etc/nginx/sites-available/ra2web <<'EOF'
server {
    listen 80;
    server_name _;
    root /var/www/ra2web;
    index index.html;

    types {
        application/wasm wasm;
    }

    location / {
        try_files $uri $uri/ =404;
        add_header Cache-Control "no-store";
    }
}
EOF

# 4. 启用并重启
sudo ln -sf /etc/nginx/sites-available/ra2web /etc/nginx/sites-enabled/
sudo rm -f /etc/nginx/sites-enabled/default
sudo nginx -t && sudo systemctl reload nginx
```

然后浏览器访问 `http://<服务器IP>/` 即可。

## 部署方式 C：本地 Windows 跑一下

任选其一，在本仓库目录下执行：

```powershell
npx serve .          # 需要 Node.js
# 或
python -m http.server 8080
```

然后访问提示的地址（如 http://localhost:8080）。

---

## 自建联机大厅（和朋友在线对战）

本部署的 `servers.json` 已内置 **"自建大厅(噩梦AI)"** 大区（置顶）。要让它工作，需要在你的服务器上运行配套的大厅服务端 [ra2web-wol-server](https://github.com/leoandagents/ra2web-wol-server)。

> 工作原理：大厅服务端 = 注册接口 + WOL 大厅（建房/聊天）+ gserv 游戏数据中继，全部走你现有站点的 443 端口由 Nginx 反代，**无需开放任何新端口**。

### 1. 安装 Node.js（18+）

```bash
node -v   # 有版本号则跳过
# Ubuntu/Debian 没有的话：
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt install -y nodejs
```

### 2. 部署大厅服务端

```bash
sudo git clone https://github.com/leoandagents/ra2web-wol-server.git /var/www/wol-server
cd /var/www/wol-server
sudo npm install --omit=dev
```

### 3. 配置 systemd 常驻

```bash
sudo tee /etc/systemd/system/ra2web-wol.service > /dev/null <<'EOF'
[Unit]
Description=ra2web self-hosted WOL lobby
After=network.target

[Service]
WorkingDirectory=/var/www/wol-server
Environment=PORT=8901
Environment=GSERV_PUBLIC_URL=wss://ra2web.yanghui.name/gserv
ExecStart=/usr/bin/node src/index.js
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl daemon-reload
sudo systemctl enable --now ra2web-wol
sudo systemctl status ra2web-wol   # 看到 active (running) 即成功
```

⚠️ **如果你用的域名不是 ra2web.yanghui.name**：把 `GSERV_PUBLIC_URL` 和本仓库 `servers.json` 里的域名都换成你的，然后重新部署站点。

### 4. Nginx 反代（关键步骤）

在你现有站点的 Nginx `server {}` 配置块里**添加**：

```nginx
# 大厅 WebSocket（登录/建房/聊天）
location /wol {
    proxy_pass http://127.0.0.1:8901;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_read_timeout 7200s;   # 一局游戏可能几小时，必须加长
}

# 游戏数据中继 WebSocket
location /gserv {
    proxy_pass http://127.0.0.1:8901;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_read_timeout 7200s;
}

# 注册接口
location = /register {
    proxy_pass http://127.0.0.1:8901;
}
```

然后 `sudo nginx -t && sudo systemctl reload nginx`。

### 5. 验证

1. 浏览器打开你的站点，登录页选 **"自建大厅(噩梦AI)"** 大区——应显示**在线**
2. 两个浏览器窗口各注册一个账号
3. 一个建房间，另一个在大厅列表加入，都点接受，房主开始游戏
4. 联机房间里可以加电脑 AI（就是本部署的增强版四级 AI）

### 常见问题（联机）

- **大区显示"离线"**：检查 `sudo systemctl status ra2web-wol` 和 Nginx 是否 reload；浏览器 F12 Network 看 `/wol` 的 WebSocket 是否 101
- **能登录但开不了局**：确认 `GSERV_PUBLIC_URL` 是 `wss://你的域名/gserv`（不是 ws://，不是 localhost），改完 `sudo systemctl restart ra2web-wol`
- **游戏中断线**：Nginx 的 `proxy_read_timeout` 必须 ≥7200s
- **账号数据**：存在服务器的 `/var/www/wol-server/accounts.json`，密码加盐哈希存储；备份这个文件即备份账号
- **备用方案**：如果自建大厅暂时不可用，可改 hosts（`<服务器IP>  game.ra2web.com`）走 ra2web 官方大厅联机；联机房带电脑 AI 时所有人必须用同一客户端，否则不同步掉线

## 常见问题

- **页面卡在"加载检测未通过"**：用 Git 克隆时注意 Windows 的换行符转换会破坏文件校验，执行 `git config --global core.autocrlf false` 后重新克隆。
- **改了文件刷新没变化**：F12 → Application → Storage → Clear site data，再刷新。
- **游戏资源下载失败**：点「点此自动导入」使用完整资源包。

## 合规说明（请遵守）

- 本项目仅供个人学习交流，**禁止任何商业用途**（收费、广告、盈利性托管）
- 部署时请保留「RA2WEB / 红色井界 / 王二火大」名称标识
- 游戏美术素材版权归 EA 所有；引擎为 Chrono Divide 作者所有
- 增强 AI 基于 [Supalosa 的开源 bot](https://github.com/Supalosa/supalosa-chronodivide-bot) 修改
